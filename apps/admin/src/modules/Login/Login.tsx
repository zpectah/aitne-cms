import { ViewLayout } from '../../components';

const Login = () => {
  console.log('page view: Login');

  return <ViewLayout meta={{ title: 'Login' }}>...Login...</ViewLayout>;
};

export default Login;
