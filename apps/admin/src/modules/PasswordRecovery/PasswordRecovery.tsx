import { ViewLayout } from '../../components';

const PasswordRecovery = () => (
  <ViewLayout isCentered meta={{ title: 'Password recovery' }}>
    ...Password recovery...
  </ViewLayout>
);

export default PasswordRecovery;
